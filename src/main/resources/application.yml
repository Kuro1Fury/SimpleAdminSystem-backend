server:
  port: 9999

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1/spring?characterEncoding=utf8&useSSL=false
#    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    port: 6379
    host: localhost

logging:
  level:
    com.example: debug

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-not-delete-value: 0
      logic-delete-value: 1
